import os

# Basic repositry of constants for usage throughout the system

ROOT_DIRECTORY = os.path.dirname(os.path.abspath(__file__))
FILE_PATH = os.path.dirname("files/")

FILE_DIRECTORY = os.path.join(ROOT_DIRECTORY, FILE_PATH)